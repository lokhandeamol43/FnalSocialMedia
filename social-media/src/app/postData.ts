const postData = {
    title: this.title,
    content: this.content,
    image: this.selectedFile.name
  };
  
//   this._httpClient.post(this.url + 'posts.json', JSON.stringify(postData), httpOptions);
  